---
name: agentmail
description: Connect to a user's AgentMail inbox to read and summarise their emails. Handles onboarding (asking for API key on first use), listing threads, reading messages, and searching. Credentials are stored per-agent-group so each user only accesses their own inbox.
---
